# =============================================================================
# HVAS (Hybrid Video Analysis System) — Python Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# Note: torch/torchvision are pulled in by ultralytics automatically.
#       Only direct dependencies are listed here; pip resolves transitive deps.
#
# GPU Support:
#   ultralytics installs CPU-only torch by default. For CUDA GPU acceleration:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   Then set env: YOLO_DEVICE=cuda
# =============================================================================

# --- Web Framework ---
flask==3.1.1
flask-cors==5.0.1

# --- ML / Computer Vision ---
ultralytics==8.3.202            # YOLOv8 (brings torch, torchvision, numpy, pillow, etc.)
opencv-python==4.12.0.88        # Video capture & image processing

# --- Data Validation ---
pydantic==2.12.3                # Event model validation

# --- Storage ---
pymongo==4.15.3                 # MongoDB driver
minio==7.2.18                   # MinIO object storage

# --- HTTP ---
requests==2.32.5                # Qwen-VL API calls, outbound HTTP

# --- File Monitoring ---
watchdog==6.0.0                 # Local video file watcher

